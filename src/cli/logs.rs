pub async fn execute(
    _session: &str,
    _target: Option<&str>,
    _tail: usize,
    _follow: bool,
    _stderr: bool,
    _all: bool,
    _timeout: Option<u64>,
) -> i32 {
    eprintln!("not yet implemented");
    1
}
