pub async fn execute(
    _session: &str,
    _target: &str,
    _until: Option<String>,
    _regex: bool,
    _exit: bool,
    _timeout: Option<u64>,
) -> i32 {
    eprintln!("not yet implemented");
    1
}
